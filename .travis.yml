sudo: false
dist: trusty

# blocklist
branches:
    except:
    - /^release.*/
    - /^feature.*/

# safelist
#branches:
#  only:
#  - master
#  - develop
#  - /^v\d+\.\d+\.\d+$/

sudo: false
dist: trusty

# blocklist
branches:
  except:
    - /^release.*/
    - /^feature.*/

# safelist
branches:
  only:
    - master
    - develop
    - /^v\d+\.\d+\.\d+$/

language: java
jdk:
    - oraclejdk8

script:
  - chmod +x  ci/script/demo-ci.sh
  - ci/script/demo-ci.sh